from tools import data
from app.models import SSTImage as SST

# VIRS files, use instrumed_id = 0 for VIRS
#files = data.file_names(instrument_id=0)

#mat = data.mat_generator(files).next()
#sst = mat['sst']
#sst = SST(sst)

#print sst.sea_ice_area()
